.env
*.log

# Go — `go build ./...` from repo root drops one binary per main package here,
# named after its directory (seed, api, ingestion, stream-processor, ...).
/bin/
/seed
/api
/ingestion
/stream-processor
/anomaly-detector
/alert-service
*.test
*.out
vendor/

# Node / frontend
node_modules/
.next/
out/
*.tsbuildinfo

# IDE / OS
.DS_Store
.idea/
.vscode/

# Docker / data volumes mounted locally (none expected, safety net)
*.pid

# Local scratch
tmp/
